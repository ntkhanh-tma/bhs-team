---
name: feedback_no_auto_commit
description: User wants to handle all git commits themselves — never commit automatically
metadata:
  type: feedback
---

Never run `git commit`, `git push`, or any git write command without being explicitly asked.

**Why:** User prefers to review and commit changes themselves.

**How to apply:** After making code changes, stop. Do not stage, commit, or push. Wait for the user to do it.
